; Development overrides — mounted via docker-compose.override.yml.
; Opcache checks file timestamps on every request so source edits
; are picked up immediately without rebuilding the image.
opcache.validate_timestamps = 1
opcache.revalidate_freq = 0
